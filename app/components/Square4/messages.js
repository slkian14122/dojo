/*
 * Square4 Messages
 *
 * This contains all the text for the Square4 component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.Square4';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Square4 component!',
  },
});
