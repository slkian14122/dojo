/*
 * Square Messages
 *
 * This contains all the text for the Square component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.Square';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Square component!',
  },
});
