/*
 * Square2 Messages
 *
 * This contains all the text for the Square2 component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.Square2';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Square2 component!',
  },
});
